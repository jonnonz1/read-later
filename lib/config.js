/**
 * Where the share viewer page lives.
 *
 * Default: the viewer shipped inside the extension. This only works for
 * recipients who also have the extension installed.
 *
 * To share with anyone on the open web, host `share/viewer.html` on a static
 * host (GitHub Pages, Netlify, etc.) and set PUBLIC_VIEWER_URL below to the
 * public URL — e.g. "https://yourname.github.io/read-later/viewer.html".
 * Share links will then point to your hosted page.
 */
export const PUBLIC_VIEWER_URL = "https://jonnonz1.github.io/read-later/share/viewer.html";
